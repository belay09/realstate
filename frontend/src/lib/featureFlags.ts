/**
 * Public UX feature flags.
 * Keep calculator source restorable - flip to true to re-enable public mounts.
 */
export const SHOW_PUBLIC_CALCULATOR = false

/**
 * Admin advanced surfaces (Properties, Promotions, legacy home cards, payment tiers).
 * Phase4+ slim: keep false so CMS matches the public 4-page flow.
 * Flip to true to restore advanced admin nav/routes without deleting files.
 */
export const SHOW_ADMIN_ADVANCED = false
